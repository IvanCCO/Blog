import { Inter, Roboto_Mono, Itim } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const itim = Itim({
  weight: "400",
  subsets: ["latin"],
  variable: "--font-itim",
});
