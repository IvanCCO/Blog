import { Inter, Itim } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const itim = Itim({
  weight: "400",
  subsets: ["latin"],
  variable: "--font-itim",
  display: "swap",
});
